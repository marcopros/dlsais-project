```diff
- API KEY required
(!!!) Ensure to have set 'OPENAI_API_KEY' in .env file
```

# 1) Install requirements
```bash
pip install -r requirements.txt
```
# 2) To run the agent
```bash
python agent.py
```